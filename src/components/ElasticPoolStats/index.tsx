import styled from 'styled-components'

const Wrapper = styled.div`
  background: ${({ theme }) => theme.buttonBlack};
`
export default function ElasticPoolStats({ poolAddress }: { poolAddress: string }) {
  return <Wrapper>sss</Wrapper>
}
