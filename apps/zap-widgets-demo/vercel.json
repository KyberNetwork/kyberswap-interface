{
  "installCommand": "corepack enable && pnpm -C .. install --frozen-lockfile",
  "buildCommand": "pnpm --filter \"./apps/zap-widgets-demo...\" build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
